name: Deploy page

on:
  push:
   branches: ['master']

jobs:
  build_master:
   runs-on: ubuntu-latest
   steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-node
      with:
       node-version: 12
    - run: npm ci
    - run: npm run deploy
